import 'package:flutter/cupertino.dart';

class Fragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Tab Bar With Fragment"),
      ),
    );
  }
}
